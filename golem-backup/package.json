{
  "name": "golem-backup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/backup.ts",
    "backup": "bun run src/backup.ts",
    "verify": "bun run src/verify.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun build ./src/backup.ts --outdir ./dist",
    "build:tsc": "tsc",
    "start:node": "node dist/backup.js"
  },
  "dependencies": {
    "golem-base-sdk": "^0.1.16",
    "dotenv": "^16.4.5",
    "tslib": "^2.8.1",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
